[project]
name = "hm-tracker-backend"
version = "0.1.0"
description = "Half-marathon training tracker backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 110
target-version = "py311"
